usage: aws-kinesis-consumer --stream-name STREAM-NAME

AWS Kinesis consumer CLI. Consume records using the stream name, not the
shards ids.

optional arguments:
  -h, --help            show this help message and exit
  --endpoint ENDPOINT   URL of the AWS Kinesis Data Stream endpoint
  --iterator-type {latest,trim-horizon}
                        Shard iterator type which defines the shard position
                        from which to start reading data records sequentially.
                        "latest" gets the new records only. "trim-horizon"
                        gets all the records from the beginning. (default:
                        latest)

required named arguments:
  --stream-name STREAM_NAME
                        Name of the Kinesis Data Stream
